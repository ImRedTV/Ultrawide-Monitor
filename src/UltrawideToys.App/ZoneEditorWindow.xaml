<?xml version="1.0" encoding="utf-8"?>
<Window x:Class="UltrawideToys.ZoneEditorWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" WindowStyle="None" ResizeMode="NoResize" ShowInTaskbar="False" Topmost="True" Background="#B2000000" AllowsTransparency="True" Title="Modifier les zones" Icon="pack://application:,,,/assets/ultrawidemonitor.ico" xmlns:ultrawidetoys="clr-namespace:UltrawideToys;assembly=UltrawideMonitor">
  <FrameworkElement.Resources>
    <ResourceDictionary>
      <Style x:Key="EditorToolbarButton" TargetType="{x:Type Button}">
        <Setter Property="Margin" Value="4,0,0,0" />
        <Setter Property="Padding" Value="12,0" />
        <Setter Property="Height" Value="32" />
        <Setter Property="MinWidth" Value="88" />
        <Setter Property="Background" Value="#273241" />
        <Setter Property="Foreground" Value="#FFFFFF" />
        <Setter Property="BorderBrush" Value="#5D6B7B" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="FontFamily" Value="Segoe UI Variable" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="FrameworkElement.OverridesDefaultStyle" Value="True" />
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="{x:Type Button}">
              <Border Name="Chrome" Background="{TemplateBinding Control.Background}" BorderBrush="{TemplateBinding Control.BorderBrush}" BorderThickness="{TemplateBinding Control.BorderThickness}" CornerRadius="4" SnapsToDevicePixels="True">
                <ContentPresenter Content="{TemplateBinding ContentControl.Content}" ContentTemplate="{TemplateBinding ContentControl.ContentTemplate}" ContentStringFormat="{TemplateBinding ContentControl.ContentStringFormat}" HorizontalAlignment="{TemplateBinding Control.HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding Control.VerticalContentAlignment}" Margin="{TemplateBinding Control.Padding}" TextElement.Foreground="{TemplateBinding Control.Foreground}" TextElement.FontFamily="{TemplateBinding Control.FontFamily}" TextElement.FontSize="{TemplateBinding Control.FontSize}" TextElement.FontWeight="{TemplateBinding Control.FontWeight}" RecognizesAccessKey="True" />
              </Border>
              <ControlTemplate.Triggers>
                <Trigger Property="UIElement.IsMouseOver" Value="True">
                  <Setter TargetName="Chrome" Property="Border.Background" Value="#35465A" />
                  <Setter TargetName="Chrome" Property="Border.BorderBrush" Value="#82B9E8" />
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                  <Setter TargetName="Chrome" Property="Border.Background" Value="#0B5AA5" />
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                  <Setter TargetName="Chrome" Property="UIElement.Opacity" Value="0.45" />
                </Trigger>
              </ControlTemplate.Triggers>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Style>
      <Style x:Key="EditorApplyButton" TargetType="{x:Type Button}" BasedOn="{StaticResource EditorToolbarButton}">
        <Setter Property="MinWidth" Value="126" />
        <Setter Property="Background" Value="#0F6CBD" />
        <Setter Property="BorderBrush" Value="#66C7FF" />
      </Style>
      <Style x:Key="EditorActionButton" TargetType="{x:Type Button}" BasedOn="{StaticResource EditorToolbarButton}">
        <Setter Property="Margin" Value="2" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="MinWidth" Value="0" />
        <Setter Property="Width" Value="36" />
        <Setter Property="Height" Value="34" />
        <Setter Property="Background" Value="#0F6CBD" />
        <Setter Property="BorderBrush" Value="#66C7FF" />
      </Style>
      <Style x:Key="EditorToolbarField" TargetType="{x:Type Control}">
        <Setter Property="Margin" Value="4,0,0,0" />
        <Setter Property="Padding" Value="10,0" />
        <Setter Property="Height" Value="32" />
        <Setter Property="Background" Value="#202A36" />
        <Setter Property="Foreground" Value="#FFFFFF" />
        <Setter Property="BorderBrush" Value="#5D6B7B" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="FontFamily" Value="Segoe UI Variable" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
      </Style>
      <Style x:Key="EditorToolbarTextBox" TargetType="{x:Type TextBox}" BasedOn="{StaticResource EditorToolbarField}">
        <Setter Property="FrameworkElement.OverridesDefaultStyle" Value="True" />
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="{x:Type TextBox}">
              <Border Name="Chrome" Background="{TemplateBinding Control.Background}" BorderBrush="{TemplateBinding Control.BorderBrush}" BorderThickness="{TemplateBinding Control.BorderThickness}" CornerRadius="4" SnapsToDevicePixels="True">
                <ScrollViewer Name="PART_ContentHost" Margin="{TemplateBinding Control.Padding}" Foreground="{TemplateBinding Control.Foreground}" FontFamily="{TemplateBinding Control.FontFamily}" FontSize="{TemplateBinding Control.FontSize}" FontWeight="{TemplateBinding Control.FontWeight}" VerticalAlignment="{TemplateBinding Control.VerticalContentAlignment}" HorizontalAlignment="{TemplateBinding Control.HorizontalContentAlignment}" VerticalScrollBarVisibility="Hidden" HorizontalScrollBarVisibility="Hidden" />
              </Border>
              <ControlTemplate.Triggers>
                <Trigger Property="UIElement.IsKeyboardFocusWithin" Value="True">
                  <Setter TargetName="Chrome" Property="Border.BorderBrush" Value="#66C7FF" />
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                  <Setter TargetName="Chrome" Property="UIElement.Opacity" Value="0.45" />
                </Trigger>
              </ControlTemplate.Triggers>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Style>
      <Style x:Key="EditorComboBox" TargetType="{x:Type ComboBox}" BasedOn="{StaticResource EditorToolbarField}">
        <Setter Property="FrameworkElement.OverridesDefaultStyle" Value="True" />
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="{x:Type ComboBox}">
              <Grid>
                <ToggleButton Name="ToggleButton" Background="{TemplateBinding Control.Background}" BorderBrush="{TemplateBinding Control.BorderBrush}" BorderThickness="{TemplateBinding Control.BorderThickness}" Foreground="{TemplateBinding Control.Foreground}" HorizontalAlignment="Left" HorizontalContentAlignment="Stretch" ClickMode="Press" Focusable="False" Width="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}" IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}">
                  <Control.Template>
                    <ControlTemplate TargetType="{x:Type ToggleButton}">
                      <Border Background="{TemplateBinding Control.Background}" BorderBrush="{TemplateBinding Control.BorderBrush}" BorderThickness="{TemplateBinding Control.BorderThickness}" CornerRadius="4" SnapsToDevicePixels="True">
                        <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />
                      </Border>
                    </ControlTemplate>
                  </Control.Template>
                  <Grid>
                    <ContentPresenter Margin="10,0,30,0" VerticalAlignment="Center" HorizontalAlignment="Left" Content="{Binding SelectionBoxItem, RelativeSource={RelativeSource AncestorType={x:Type ComboBox}}}" ContentTemplate="{Binding SelectionBoxItemTemplate, RelativeSource={RelativeSource AncestorType={x:Type ComboBox}}}" TextElement.Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType={x:Type ComboBox}}}" />
                    <Path Data="M0,0 L10,0 L5,5" Fill="{TemplateBinding Control.Foreground}" Width="10" Height="5" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,8,0" />
                  </Grid>
                </ToggleButton>
                <Popup Name="PART_Popup" Placement="Bottom" IsOpen="{TemplateBinding ComboBox.IsDropDownOpen}" AllowsTransparency="True" Focusable="False" StaysOpen="False" PlacementTarget="{Binding ElementName=ToggleButton}">
                  <Border Background="#202A36" BorderBrush="#5D6B7B" BorderThickness="1" MinWidth="{Binding ActualWidth, ElementName=ToggleButton}">
                    <ScrollViewer MaxHeight="360" CanContentScroll="True">
                      <ItemsPresenter />
                    </ScrollViewer>
                  </Border>
                </Popup>
              </Grid>
              <ControlTemplate.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                  <Setter TargetName="ToggleButton" Property="UIElement.Opacity" Value="0.55" />
                </Trigger>
              </ControlTemplate.Triggers>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Style>
      <Style x:Key="EditorComboBoxItem" TargetType="{x:Type ComboBoxItem}">
        <Setter Property="Height" Value="32" />
        <Setter Property="Padding" Value="10,0" />
        <Setter Property="Foreground" Value="#FFFFFF" />
        <Setter Property="Background" Value="#202A36" />
        <Setter Property="FontFamily" Value="Segoe UI Variable" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Style.Triggers>
          <Trigger Property="ComboBoxItem.IsHighlighted" Value="True">
            <Setter Property="Background" Value="#0078D4" />
          </Trigger>
        </Style.Triggers>
      </Style>
    </ResourceDictionary>
  </FrameworkElement.Resources>
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="58" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>
    <Border Grid.Row="0" Background="#E61B2530" Padding="14,8">
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
          <TextBlock Name="MonitorTitle" FontSize="12" FontWeight="SemiBold" VerticalAlignment="Center" Foreground="#FFFFFF" TextTrimming="CharacterEllipsis" MaxWidth="340" />
          <TextBlock Text="  ·  " Opacity=".70" VerticalAlignment="Center" Foreground="#FFFFFF" FontSize="12" />
          <TextBlock Text="Preset" Width="46" TextAlignment="Right" VerticalAlignment="Center" Margin="0,0,8,0" Foreground="#D9E7F5" FontSize="12" />
          <ComboBox Name="PresetCombo" Width="184" Height="32" VerticalAlignment="Center" Style="{StaticResource EditorComboBox}" ItemContainerStyle="{StaticResource EditorComboBoxItem}" Background="#202A36" Foreground="#FFFFFF" BorderBrush="#5D6B7B">
            <ItemsControl.ItemTemplate>
              <DataTemplate>
                <TextBlock Foreground="#FFFFFF" FontFamily="Segoe UI Variable" FontSize="12" VerticalAlignment="Center" Text="{Binding Name}" />
              </DataTemplate>
            </ItemsControl.ItemTemplate>
          </ComboBox>
          <TextBox Name="PresetName" Width="184" Height="32" VerticalAlignment="Center" ToolTip="Nom du preset personnalisé" Style="{StaticResource EditorToolbarTextBox}" />
          <Button Name="SavePresetButton" Width="104" Content="Enregistrer" Style="{StaticResource EditorToolbarButton}" Click="SavePreset_Click" />
          <Button Name="DeletePresetButton" Width="96" Content="Supprimer" Visibility="Collapsed" IsEnabled="False" Style="{StaticResource EditorToolbarButton}" Click="DeletePreset_Click" />
          <TextBlock Name="EditorStatus" Margin="10,0,0,0" VerticalAlignment="Center" Foreground="#8DE8A0" FontSize="12" TextTrimming="CharacterEllipsis" MaxWidth="240" />
        </StackPanel>
        <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="16,0,0,0">
          <Button Name="ResetButton" Width="110" Content="Réinitialiser" Style="{StaticResource EditorToolbarButton}" Click="Reset_Click" />
          <Button Name="CancelButton" Width="90" Content="Annuler" Style="{StaticResource EditorToolbarButton}" Click="Cancel_Click" />
          <Button Name="ApplyButton" Width="126" Content="Utiliser les zones" Style="{StaticResource EditorApplyButton}" Click="Apply_Click" />
        </StackPanel>
      </Grid>
    </Border>
    <Canvas Name="EditorCanvas" Grid.Row="1" Background="#52000000" SizeChanged="EditorCanvas_SizeChanged" />
  </Grid>
</Window>
